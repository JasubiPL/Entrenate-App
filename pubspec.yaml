name: entrenate
description: "Aplicación de enternamiento"
publish_to: 'none'
version: 0.3.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  fonts:
  - family: titles
    fonts:
      - asset: assets/fonts/oswald.ttf

  assets:
    - assets/img/
    - assets/img/icons/
    - assets/img/routines/